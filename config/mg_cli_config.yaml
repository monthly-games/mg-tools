# MG-CLI Configuration
# Generated by mg-cli config init

environment: dev

firebase:
  project_pattern: mg-game-{game_id}
  shared_project: mg-games-dev
  use_shared: true

ads:
  sdk: admob
  test_mode: true
  mediation: false

batch:
  parallel: false
  max_workers: 4
  dry_run: false

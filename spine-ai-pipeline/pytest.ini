[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    requires_gpu: Tests requiring GPU acceleration (skipped on CPU-only CI)
    e2e: End-to-end tests requiring full pipeline
    slow: Tests taking more than 30 seconds
